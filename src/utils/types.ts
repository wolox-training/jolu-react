export type Nullable<T> = T | null;
export interface DataRegister {
  email: string;
  first_name: string;
  last_name: string;
  password: string;
  password_confirmation: string;
  locale: string;
}
